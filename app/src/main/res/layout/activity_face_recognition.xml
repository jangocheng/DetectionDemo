<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.RecognitionActivity">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <Button
                android:id="@+id/person_manage"
                android:text="@string/person_manage"
                style="@style/MyButton" />
            <Button
                android:id="@+id/group_manage"
                android:text="@string/group_manage"
                style="@style/MyButton" />
            <Button
                android:id="@+id/training_person"
                android:text="@string/training_person"
                style="@style/MyButton" />
            <Button
                android:id="@+id/training_group"
                android:text="@string/training_group"
                style="@style/MyButton" />
            <Button
                android:id="@+id/train_state"
                android:text="@string/training_state"
                style="@style/MyButton" />
            <Button
                android:id="@+id/recognition_two_face"
                android:text="@string/tow_person_recognition"
                style="@style/MyButton"  />
        </LinearLayout>
    </ScrollView>
</RelativeLayout>
